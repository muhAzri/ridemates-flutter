org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Some plugins (e.g. flutter_image_compress_lite) declare Java target 1.8 but let
# Kotlin default to the build JDK's target (21), which AGP rejects as inconsistent.
# Android dexes every module (D8/R8) regardless of per-module JVM target, so the
# mismatch is harmless — downgrade the check from error to warning so the build
# proceeds. (The real fix belongs upstream: the plugin should pin its jvmTarget.)
kotlin.jvm.target.validation.mode=warning
